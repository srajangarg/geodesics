basic

- base code for input and storage
- algorithm for djikstras/ subdivision
- mmp algorithm
	
	data structs required :

		window class
		a map from edges to the existing windows it has on it
		a heap for inserting/removing the edges (std::set)

	functions required :

		back tracing function which gives path

- visualization of shortest path
- geodseci contour visualiztion
- interactive point selection

extra

- handle non-triangle face meshes (essentially triangulation)
- make a comparision suite, shwo extreme cases, speedups etc.